#include <stdlib.h>
#include <stdio.h>

/*
 * main - Entry point
 * @argc: input
 * @argv: input
 * Return: Always 0 (Succes)
 */
int main(int argc, __attribute__((unused)) char **argv)
{
	printf("%d\n", argc - 1);
	return (0);
}
